import PlaceholderPage from "./PlaceholderPage";
import { Settings as SettingsIcon } from "lucide-react";

export default function Settings() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Configure app preferences, AI coaching settings, and data management options."
      icon={SettingsIcon}
    />
  );
}
